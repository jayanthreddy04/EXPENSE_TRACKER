{
  "name": "expense-tracker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "build": "npm run build --prefix client",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix server",
    "verify": "npm run build && node --check server/services/ocrService.js && node --check server/app.js && node --check api/index.js",
    "start": "npm run start --prefix server"
  }
}
